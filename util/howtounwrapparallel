1.  Create the int amp and cc files using ~/alosdgx/run_dither.py

2.  If desired look down these by creating a subdirectory as in this example

    mkdir 4x4
    cd 4x4
    ~/util/4x4.py width looks

3.  remove 4x4 suffixes 

    ~/util/strip4x4.py

4.  get the intlist, split it into multiple lists

    ls -1 *int | cat > intlist
    ~/util/splitintlist.py

5.  make a local copy of ~/util/rununwrap

    edit to have correct width for these files and choose a box size

6.  unwrap using ./rununwrap

